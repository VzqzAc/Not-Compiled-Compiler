%p#notice= notice
%h1 Listing Compilers
%table
  %thead
    %tr
      %th Source code
      %th{:colspan => "3"}
  %tbody
    - @compilers.each do |compiler|
      %tr
        %td= compiler.source_code
        %td= link_to 'Show', compiler
        %td= link_to 'Edit', edit_compiler_path(compiler)
        %td= link_to 'Destroy', compiler, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Compiler', new_compiler_path
